name = "priorizai"
main = "index.js"
compatibility_date = "2026-02-12"
preview_urls = true

# KV para rate limit diário por IP
[[kv_namespaces]]
binding = "RATE_LIMIT_KV"
id = "4cf4b8ee186f4951a69a536b429c6943"
preview_id = "9ddad3d8a3fd4658831c2d0fa65617a6"

# Variáveis NÃO secretas (configure no Cloudflare Workers ou aqui, se preferir)
# [vars]
# OPENAI_MODEL = "gpt-4o-mini"

# Secrets (configure via Cloudflare Dashboard ou wrangler):
# - OPENAI_API_KEY
# - IP_DAILY_LIMIT
# - HASH_SALT
